(function ( $, lesson ){

lesson.plugins = {
  exercise_1: function() {
    /*

        Create a plugin that will add a checkbox before each task item and hide the text delete button.  Each checkbox should have an event that turns the text gray when selected.

    */

  }
};

})( jQuery, window.lesson = window.lesson || {} );